const apiVersion = 2

export default {
	version: apiVersion,
	prefix: `/api/v${apiVersion}/admin`,
	filePrefix: `/api/v${apiVersion}/file/`,
	mainImagePrefix: `/api/v${apiVersion}/file/main-image`,
	path: {
		categoryDetail: '/product-category/detail',
	},
}
