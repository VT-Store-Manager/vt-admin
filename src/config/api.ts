const apiVersion = 2

export default {
	version: apiVersion,
	prefix: `/api/v${apiVersion}/admin`,
	filePrefix: '/api/file/',
	mainImagePrefix: '/api/file/main-image',
	path: {
		categoryDetail: '/product-category/detail',
	},
}
